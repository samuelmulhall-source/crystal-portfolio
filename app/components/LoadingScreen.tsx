"use client";

/**
 * LoadingScreen — placeholder component.
 *
 * Currently disabled (returns null). The starfield itself serves as the
 * visual loading experience via orbital particle animation controlled
 * by voidState.modelLoading in VoidBackground.
 */

export default function LoadingScreen() {
  return null;
}
